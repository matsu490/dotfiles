[seqdata_rev] = f_classize( seqdata )
% Classize to SA & EA 

idHead = find(seqdata(:,3)==1);
idSA = idHead(find(seqdata(idHead,1)<=1000));
idEA = idHead(find(seqdata(idHead,1)>=1000));
seqdata(:,4) = 0;
seqdata(idSA,4) = 1;
seqdata(idEA,4) = 2;
